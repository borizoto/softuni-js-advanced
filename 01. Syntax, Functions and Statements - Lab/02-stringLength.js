function stringLength(string1, string2, string3) {
    const sum = string1.length + string2.length + string3.length;
    const avg = Math.floor(sum / 3);
    console.log(sum);
    console.log(avg);
 }